{
  "name": "hart-lending",
  "version": "1.0.0",
  "private": true,
  "description": "Hart Lending static marketing site",
  "scripts": {
    "dev": "live-server --port=8000 --no-browser",
    "start": "live-server --port=8000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
